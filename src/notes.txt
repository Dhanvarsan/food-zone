useEffect() => will be called after component rendered
render(application [shimmer UI / loader]) -> apicall (where use effect triggers) -> render (actual data with UI)
Shimmer UI - rendering  a skeleton UI before we get the api datas. [new standard of showing loadind page]
Conditional rendering - rendering on the basis of condition. 
[eg:]
if(!Data) shimmerUI;
else rest BodyComponent will render;

useEffect() => if dependency array is not present => it will be called in every render.
useEffect() => if dependency array is empty [] => it will be called only once on component render.
useState() => should not be used inside conditions
